{
  "name": "portfolio-dynamique",
  "version": "1.0.0",
  "scripts": {
    "start": "node dist/server/index.js",
    "build": "tsc -p ./client/tsconfig.json && tsc -p ./server/tsconfig.json && tsc -p ./admin/tsconfig.json",
    "dev": "tsc --watch & nodemon dist/server/index.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
